[package]
name = "homecare-gap-fill"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["std"] }
fold = { path = "../../../fold" }
serde = { version = "1", features = ["derive"] }
