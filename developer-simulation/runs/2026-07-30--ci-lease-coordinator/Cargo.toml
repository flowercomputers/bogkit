[package]
name = "ci-lease-coordinator"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[dependencies]
fold = { path = "../../../fold" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "allow"
