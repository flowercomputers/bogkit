[package]
name = "provenance-revocation-reproducer"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
fold = { path = "../../../fold" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
