[package]
name = "bog-definition"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
schemars = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"

[dev-dependencies]
jsonschema = { version = "0.56", default-features = false }
