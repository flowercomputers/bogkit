{
  "name": "tompkins-audio-search-tools",
  "private": true,
  "type": "module",
  "description": "Bounded, read-only S3 access to the Oda archive. The Intel aws CLI at /usr/local/aws-cli hangs on this machine, so everything goes through the JS SDK with the shared-credentials provider.",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/credential-providers": "^3.700.0"
  }
}
