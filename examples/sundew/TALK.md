# Sundew · Parallax — 90 seconds

Memorize. Do not improvise a product tour.
Phone timer. If wifi dies: `cargo run -p sundew -- --script`.

One thread: **compile models early; choose the retrieval goal late.**
Parallax is the thesis. Sundew Replay is the proof that views stay current when records change. Do not present two products.

**Full briefing first.** Open http://localhost:3012 (or :3000 if that process is current), press `f` or click **Full briefing**, then start the timer.

- **0:00–0:12** — Compile models early. Choose the goal late. Embeddings are views, not a map of reality. Not a router — a mutable mix of retrievers.
- **0:12–0:22** — One KeyedStream. ESE 512d and Potion 256d. Question + goal → rank fusion. Spaces stay separate.
- **0:22–0:42** — Ask is already “refresh tokens.” Point at stale wiki + bad design. Question + goal.
- **0:42–0:62** — Comment lands. It jumps to #1. “Records change. Every view resticks.”
- **0:62–0:72** — Wiki retracts. Ghost says BM25 + HNSW. “One remove. Every index.”
- **0:72–0:85** — Same question. New goal. Winner flips. Weighted rank fusion. Spaces stay separate.
- **0:85–0:90** — **Copy briefing.** “The next session gets the current views.” Sit down.

Pitch: *Parallax is not a new routing algorithm. It is an experiment in using a mutable database runtime to keep a mixture of retrievers current.*
