# Sundew — 90 seconds

Memorize. Do not improvise a product tour.
Phone timer. If wifi dies: `cargo run -p sundew -- --script`.

**Full briefing first.** Open http://localhost:3012 (or :3000 if that process is current), press `f` or click **Full briefing**, then start the timer.

- **0:00–0:10** — Agents don’t need another grep. They need the 2k tokens that are true right now.
- **0:10–0:25** — Alinery’s filesystem is the database. Sundew is the query engine. Hit **Replay**.
- **0:25–0:45** — Ask is already “refresh tokens.” Point at stale wiki + bad design in the pack. Token bar.
- **0:45–0:70** — Comment lands. It jumps to #1. “Comments are Alinery’s product loop. Fold restuck the briefing.”
- **0:70–0:85** — Wiki retracts. Ghost says BM25 + HNSW. “One remove. Every view.”
- **0:85–0:90** — **Copy briefing.** “This is what the next session would get. We did not beat Séance at git search.” Sit down.

Pitch to preserve: *Alinery’s filesystem is the database. Sundew is the query engine it doesn’t have yet.*
